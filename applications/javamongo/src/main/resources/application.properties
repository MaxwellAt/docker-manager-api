# Configura��o do MongoDB
#spring.data.mongodb.uri = ${DB_HOST}://${DB_NAME}:27017/db-javamongo
# spring.data.mongodb.uri=mongodb://database:27017/db-javamongo
#spring.data.mongodb.uri=mongodb://${DB_USERNAME}:${DB_PASSWORD}@{DB_HOST}:27017/${DB_NAME}
spring.data.mongodb.uri=${ME_CONFIG_MONGODB_URL}${DB_NAME}
# Configura��o do servidor Spring Boot
server.port=8082

# spring.data.mongodb.host=${DB_HOST}
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=${DB_NAME}
# spring.data.mongodb.username=${DB_USERNAME}
# spring.data.mongodb.password=${DB_PASSWORD}